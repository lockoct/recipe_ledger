## 1. 页面框架搭建

- [x] 1.1 创建 `recipe_detail_page.dart`，定义 `RecipeDetailPage` StatefulWidget，接收 `recipeId` 参数
- [x] 1.2 在 `initState` 中通过 `RecipeProvider` 加载菜谱数据，处理菜谱不存在的情况
- [x] 1.3 搭建整体布局骨架：顶部封面区 + 信息区 + 原材料区 + 做法区 + 注意事项区
- [x] 1.4 配置状态栏透明、图标深色样式

## 2. 封面图片展示

- [x] 2.1 实现封面区域，覆盖状态栏（使用 SliverAppBar 或 SafeArea + 负边距）
- [x] 2.2 实现网络图片封面加载（Image.network）
- [x] 2.3 实现本地文件封面加载（Image.file），包含文件存在性检查
- [x] 2.4 实现无封面时的默认图标展示（Icons.menu_book）

## 3. 浮动操作按钮

- [x] 3.1 使用 Stack + Positioned 实现右下角浮动按钮布局
- [x] 3.2 实现返回按钮：点击 `Navigator.pop`
- [x] 3.3 实现编辑按钮：点击跳转菜谱编辑页，传递 `recipeId`
- [x] 3.4 实现删除按钮：弹出确认对话框

## 4. 菜谱基本信息展示

- [x] 4.1 展示菜谱名称，限制最大行数为2
- [x] 4.2 展示更新时间，格式为"更新于 yyyy-MM-dd"

## 5. 原材料列表与成本计算

- [x] 5.1 实现原材料列表展示，区分菜品类型和自定义类型
- [x] 5.2 菜品类型原材料：通过 `DishProvider` 查询菜品名称和单价
- [x] 5.3 自定义类型原材料：显示自定义名称，不显示成本
- [x] 5.4 实现成本计算：成本 = 菜品单价 × (用量 / 换算系数)，统一转换为"斤"
- [x] 5.5 实现合计成本展示（红色字体）
- [x] 5.6 实现无原材料时的空状态提示

## 6. 富文本内容展示

- [x] 6.1 使用 flutter_quill `QuillEditor` 只读模式展示做法步骤
- [x] 6.2 使用 flutter_quill `QuillEditor` 只读模式展示注意事项
- [x] 6.3 无注意事项时隐藏该区域

## 7. 编辑返回刷新

- [x] 7.1 编辑页跳转使用 `await Navigator.push`，返回后检测刷新信号
- [x] 7.2 编辑页保存后 `pop(true)`，详情页检测后重新加载菜谱数据

## 8. 删除功能

- [x] 8.1 实现确认删除 AlertDialog，包含"确认删除"和"取消"按钮
- [x] 8.2 确认后调用 `RecipeProvider.deleteRecipe(recipeId)` 执行删除
- [x] 8.3 删除成功后 `Navigator.pop` 返回列表，显示"菜谱已删除" SnackBar
- [x] 8.4 删除失败时显示错误 SnackBar

## 9. 页面适配与优化

- [x] 9.1 适配不同屏幕尺寸，确保布局在不同设备上正常显示
- [x] 9.2 确保富文本渲染流畅无卡顿